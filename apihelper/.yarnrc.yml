nodeLinker: node-modules
enableScripts: false
enableImmutableInstalls: false   # local migration installs need to write the lockfile; CI uses --immutable

# Git dependencies must be explicitly approved. Empty = none (most projects have no git deps).
# Set explicitly (do NOT just omit it) — see the migration caveat below.
approvedGitRepositories: []

# Supply-chain cooldown: refuse dependency versions published less than 7 days ago,
# but trust our own first-party packages immediately.
npmMinimalAgeGate: 7d
npmPreapprovedPackages:
  - "@churchapps/*"
  - "@aws-sdk/*"        # daily publisher; transitive ^x.y.z ranges leave no version aged past the gate
  - "@smithy/*"         # AWS SDK's daily-published runtime deps
