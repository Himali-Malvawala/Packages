---
"@churchapps/helpers": patch
---

Add age/grade fields to membership interfaces: `grade`/`school` on `PersonInterface` and `minAgeMonths`/`maxAgeMonths`/`minGrade`/`maxGrade` on `GroupInterface`.
