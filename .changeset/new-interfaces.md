---
"@churchapps/helpers": minor
---

Phase 4 member features: add `EventRsvpInterface` and `EventInterface.rsvpDisabled` (GR-1 event RSVP), `GroupInterface.confidential` (confidential groups), `MessageInterface.reactions` + `MessageReactionSummaryInterface`, and the `"reaction"` socket action on `SocketPayloadAction` (chat reactions realtime protocol).
